<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gigabytegirls.communityalliance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RLogin" />
        <activity android:name=".RSignup" />
        <activity android:name=".RAreaActivity" />
        <activity android:name=".RChoices" />
        <activity android:name=".ALogin" />
        <activity android:name=".AUserSpace" />
        <activity android:name=".ASignUp" />
        <activity android:name=".Individual" />
        <activity android:name=".Buisness"></activity>
    </application>

</manifest>